import * as WCColrPickr from '../../../dist/wc-colr-pickr.js';
import { TestUtils } from '../../util/test-utils.js';

describe('WCColrPickr', () => {
  it('renders component', async () => {
    console.log(WCColrPickr, TestUtils);
    // const { shadowRoot } = await TestUtils.render('wc-colr-pickr');
    // expect(shadowRoot).toBeDefined();
  });
});
